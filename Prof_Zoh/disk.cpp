#include<bits/stdc++.h>

using namespace std;

int main(){
    int v;
    cin>>v;
    string cmd;
    list<int> li;
    while(1){
        cin>>cmd;
        if(cmd.compare("end")) break;
        if(cmd.compare("write")){

        }
        else if(cmd.compare("show")){

        }
        else if(cmd.compare("delete")){

        }
    }
}